<html>

<h1>Space Station Page</h1><br>
<a href="/eve2/industry/">Industry</a><br>
<a href="/eve2/out_in_space/undock">undock</a><br>
<br/>

<form id="choose-ship" action="/eve2/space_station/" method="post">
    <label>Change Piloting ship:</label> <select name="id"> 
        <option>None</option>
        {{#each item}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
</select>

    <label></label><button onclick="changeContainer({{id}})">Change</button><br>
</form><br>

<h3>Ships and Containers:</h3>
<table>
    <thead> 
        <th><a href="/eve2/out_in_space/Name">Name</a></th> 
        <th><a href="/eve2/out_in_space/Type">Type</a></th> 
        <th><a href="/eve2/out_in_space/Quantity">Quantity</a></th>   
        <th><a href="/eve2/out_in_space/Volume">Volume</a></th>   
        <th><a href="/eve2/out_in_space/Capacity">Capacity</a></th>   
        <th>Pilotable</th> 
        <th>Packaged?</th>
        <th>Move Item</th>
        <th>Give Item</th>
        <th>Delete Item</th>

    </thead> 
    <tbody> 
        {{#each CS}}
        <tr> 
            <td>{{Name}}</td>
            <td>{{Pilotable}}</td>
            <td>{{capacity}}</td>
            <td>{{type}}</td>
            <td>
            <form id="move-item" action="/item" method="get">
    <select name="id"> 
        {{#each item}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
    </select><br>
    </form>
            </td>
            <td><button onclick="deleteItem({{id}})">Give</button></td>
            <td><button onclick="deleteItem({{id}})">Delete</button></td>
            <td><button onclick="deleteItem({{id}})">Repackage</button></td>

    <table>
    <thead> 
        <th>Inside Item</th>
    </thead>
    <tbody>
        <tr>
            <td>{{Item.name}}</td>
        </tr>
    </tbody>   
    </table>

        </tr> 
        {{/each}}
    </tbody> 
</table> 
<br/>
<br/>

<form id="sort-item" action="/eve2/space_station/" method="post">
   <label>Filter by structures of type:</label> <select name="id"> 
       <option>None</option>
        {{#each item}}
        <option value="id">type</option>
        {{/each}}
    </select>

    <label></label><button onclick="selectType({{id}})">Select</button><br>
</form>

    <form id="sort-container" action="/eve2/space_station/" method="post">
   <label>Filter by cargo spaces of scale:</label> <select name="id"> 
       <option>None</option>
        {{#each item}}
        <option value="id">type</option>
        {{/each}}

</select>

    <label></label><button onclick="selectType({{id}})">Select</button><br>
</form>

</html>